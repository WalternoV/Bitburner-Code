/** @param {NS} ns */
export async function main(ns) {

  // server = ns.flags('--server', "n00dles")

  //  if (ns.fileExists("NUKE.exe")){
  //   ns.nuke(server)
  // }

  let server = ns.args[0]

  if (ns.fileExists("NUKE.exe")){
    ns.nuke(server)
    ns.print("NUKE.js executed.")
  }

}
